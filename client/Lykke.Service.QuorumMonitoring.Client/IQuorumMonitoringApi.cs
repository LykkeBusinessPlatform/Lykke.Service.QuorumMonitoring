using JetBrains.Annotations;
using Refit;

namespace Lykke.Service.QuorumMonitoring.Client
{
    // This is an example of service controller interfaces.
    // Actual interface methods must be placed here (not in IQuorumMonitoringClient interface).

    /// <summary>
    /// QuorumMonitoring client API interface.
    /// </summary>
    [PublicAPI]
    public interface IQuorumMonitoringApi
    {
    }
}
